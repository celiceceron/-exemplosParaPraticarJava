/* Somar dois numeros e retornar: A soma deu: XX
*/

function somaDeDoisNumeros(num1, num2){

    const soma = (num1 + num2);
    const retornar = "A soma deu " + soma;
    return retornar
}

console.log(somaDeDoisNumeros(5,6));